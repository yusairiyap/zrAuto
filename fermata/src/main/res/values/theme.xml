<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">

    <attr name="appMediaItemStyle" format="reference" />
    <attr name="appControlPanelStyle" format="reference" />
    <attr name="appSeekBarStyle" format="reference" />
    <attr name="appEqualizerStyle" format="reference" />
    <attr name="appStreamProgressStyle" format="reference" />
    <attr name="appQualityCardStyle" format="reference" />

    <style name="AppTheme" parent="Theme.Utils.Base">
        <item name="android:forceDarkAllowed" tools:targetApi="q">true</item>
        <item name="android:isLightTheme" tools:targetApi="q">false</item>
        <item name="android:navigationBarColor">?attr/colorPrimary</item>
        <item name="android:windowBackground">@drawable/app_background</item>
        <item name="appMediaItemStyle">@style/AppTheme.MediaItemStyle</item>
        <item name="appControlPanelStyle">@style/AppTheme.ControlPanelStyle</item>
        <item name="appSeekBarStyle">@style/AppTheme.SeekBarStyle</item>
        <item name="appEqualizerStyle">@style/AppTheme.EqualizerStyle</item>
        <item name="appStreamProgressStyle">@style/AppTheme.StreamProgressStyle</item>
        <item name="appQualityCardStyle">@style/AppTheme.QualityCardStyle</item>
        <item name="cornerRadius">5dp</item>
    </style>

    <style name="AppTheme.MediaItemStyle">
        <item name="iconColor">?attr/colorOnSecondary</item>
        <item name="hintColor">@android:color/holo_green_light</item>
        <item name="titleTextAppearance">?attr/textAppearanceListItem</item>
        <item name="subtitleTextAppearance">?attr/textAppearanceCaption</item>
        <item name="android:textColor">@color/media_item_text_color</item>
        <item name="tint">@color/media_item_tint</item>
        <item name="indicatorColor">@color/media_item_tint</item>
        <item name="elevation">3dp</item>
    </style>

    <style name="AppTheme.ControlPanelStyle">
        <item name="size">70dp</item>
        <item name="tint">?attr/colorOnPrimary</item>
        <item name="android:colorBackground">?attr/colorPrimary</item>
        <item name="textAppearance">@style/AppTheme.ControlPanelTextAppearance</item>
    </style>

    <style name="AppTheme.ControlPanelTextAppearance" parent="TextAppearance.Material3.BodyLarge">
        <item name="android:textColor">?android:attr/textColorPrimary</item>
    </style>

    <style name="AppTheme.SeekBarStyle">
        <item name="android:textColor">?android:attr/textColorPrimary</item>
        <item name="android:progressDrawable">@drawable/slider_track</item>
        <item name="android:thumb">@drawable/slider_thumb</item>
        <item name="android:splitTrack">false</item>
        <!-- Was colorOnPrimary: a role meant for content on top of the (often very light, e.g.
        Material You light) colorPrimary control-panel background, which can end up near-white
        there and barely visible against the same near-white slider track. colorControlActivated
        is what the settings sliders (Theme.Utils.Base.SliderStyle) are tinted with too, and
        matching it here keeps every slider in the app reading the same color, and it's the attr
        actually meant for "the active state of a control", so it stays legible across every
        theme. -->
        <item name="android:thumbTint">?attr/colorControlActivated</item>
        <item name="android:progressTint">?attr/colorControlActivated</item>
        <item name="android:secondaryProgressTint">?attr/colorAccent</item>
    </style>

    <style name="AppTheme.EqualizerStyle">
        <item name="android:textColor">?android:attr/textColorSecondary</item>
        <item name="tint">?attr/colorControlActivated</item>
        <!-- The "thin"/"small" variants were sized for an even older, cramped horizontal band row
        design; this style now uses the same thicker track/thumb as the main playback seek bar
        (appSeekBarStyle) for bigger, easier-to-grab faders, in both the vertical-column and the
        current horizontal-row channel layout (equalizer_channel.xml). -->
        <item name="android:progressDrawable">@drawable/slider_track</item>
        <item name="android:thumb">@drawable/slider_thumb</item>
        <item name="android:splitTrack">false</item>
        <item name="android:thumbTint">?attr/colorControlActivated</item>
        <item name="android:progressTint">?attr/colorControlActivated</item>
        <item name="android:secondaryProgressTint">?android:attr/textColorHint</item>
        <!-- SwitchCompat ignores the android:thumbTint/progressTint above (those only apply to
        SeekBar); it reads its own app:trackTint/thumbTint instead. Without an explicit opaque
        override here, the default Material "off" track renders semi-transparent by design,
        letting whatever is behind the switch show through, so force both states fully opaque. -->
        <item name="trackTint">@color/switch_track_tint</item>
        <item name="thumbTint">@color/switch_thumb_tint</item>
    </style>

    <style name="AppTheme.StreamProgressStyle">
        <item name="indicatorColor">#FFD600</item>
        <item name="trackColor">?attr/colorSecondary</item>
        <item name="trackCornerRadius">2dp</item>
    </style>

    <!-- Plan-card-style radio buttons (see equalizer_quality_channel.xml): a checkable card whose
    border highlights when it's the selected option, mirroring media_item_border.xml's own
    selected/unselected color pair. The card itself never needs to look "pressed" or float above
    the page (it's a selector, not a button), so elevation stays flat; the radio dot each card
    hosts is the primary state indicator (a shape difference, not just color), the border is a
    secondary reinforcement. -->
    <style name="AppTheme.QualityCardStyle" parent="Widget.Material3.CardView.Outlined">
        <item name="cardCornerRadius">?attr/cornerRadius</item>
        <item name="cardElevation">0dp</item>
        <item name="strokeWidth">2dp</item>
        <item name="strokeColor">@color/quality_card_stroke</item>
        <item name="cardBackgroundColor">?attr/colorSurfaceVariant</item>
        <item name="rippleColor">?attr/colorControlActivated</item>
        <!-- MaterialCardView draws its own checkmark badge over a checked card by default; each
        card here already hosts its own radio dot as the selected-state indicator, so the built-in
        badge is a redundant, distinct visual competing with it in the same corner.
        checkedIconVisible=false alone didn't suppress it on-device; setting the drawable itself
        to null is the unambiguous way to guarantee nothing renders regardless of checked state. -->
        <item name="checkedIcon">@null</item>
        <item name="checkedIconVisible">false</item>
    </style>

</resources>
